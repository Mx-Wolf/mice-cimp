import { CalcDetails } from "./calc-imp-props";
import { CalcImpServices } from "./services";
export const calcPayAgentMin = (props: CalcDetails, service: CalcImpServices): CalcDetails => {
  throw new Error();
}